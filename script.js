// SketchPad
const c=document.getElementById('sketch');if(c){const ctx=c.getContext('2d');let drawing=false,px=0,py=0;function pos(e){const r=c.getBoundingClientRect();return[(e.touches?e.touches[0].clientX:e.clientX)-r.left,(e.touches?e.touches[0].clientY:e.clientY)-r.top]}c.addEventListener('mousedown',()=>drawing=true);c.addEventListener('mouseup',()=>drawing=false);c.addEventListener('mouseleave',()=>drawing=false);c.addEventListener('mousemove',e=>{if(!drawing)return;let [x,y]=pos(e);ctx.lineWidth=2;ctx.lineCap='round';ctx.strokeStyle='#6b3fa0';ctx.beginPath();ctx.moveTo(px,py);ctx.lineTo(x,y);ctx.stroke();px=x;py=y});c.addEventListener('mousedown',e=>{[px,py]=pos(e)});function clearCanvas(){if(!c)return;ctx.clearRect(0,0,c.width,c.height)}window.clearCanvas=clearCanvas;document.getElementById('saveLink')?.addEventListener('click',function(){if(!c)return;this.href=c.toDataURL()})}
// Quotes
const quotes=["Reading is dreaming with open eyes.","A book is a garden, an orchard, a storehouse of treasures.","Books are uniquely portable magic.","There is no friend as loyal as a book."]
function nextQuote(){const el=document.getElementById('quote');if(!el)return;el.textContent=quotes[Math.floor(Math.random()*quotes.length)]}
// Contact form -> open mail client
function sendMail(e){e.preventDefault();const n=document.getElementById('name').value;const em=document.getElementById('email').value;const m=document.getElementById('message').value;window.location.href=`mailto:cherrika200@gmail.com?subject=${encodeURIComponent('Portfolio contact from '+n)}&body=${encodeURIComponent(m+'\n\nfrom: '+n+' | '+em)}`}